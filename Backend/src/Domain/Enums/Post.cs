namespace Domain.Enums;

public enum Post // должность
{
    HeadOfDepartment, // Заведующий кафедрой
    Professor, // профессор
    Docent,  // доцент
    SeniorLecturer,   // Старший преподаватель
    SeniorTeacher,    // Старший преподаватель
    Lecturer,         // Преподаватель
    Assistant // ассистент
}
